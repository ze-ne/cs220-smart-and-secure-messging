<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".frontend.activities.ConversationActivity" >

    <item
        android:id="@+id/deletion_text"
        android:title="Deletion"
        app:showAsAction="always"/>
    <item
        android:id="@+id/deletion_switch"
        android:title="Deletion Switch"
        app:actionLayout="@layout/switch_item"
        app:showAsAction="always" />
    <item
        android:id="@+id/analytics_text"
        android:title="Analytics"
        app:showAsAction="always"/>

    <item
        android:id="@+id/analytics_switch"
        android:title="Switch"
        app:actionLayout="@layout/switch_item"
        app:showAsAction="always" />
</menu>